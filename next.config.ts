import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Turbopack file system cache is enabled by default in Next.js 16
  // To use Turbopack, run: npm run dev -- --turbopack
  // or update package.json: "dev": "next dev --turbopack"
};

export default nextConfig;
